version: "3.1"

intents:
  - exchange_rate

entities:
  - currency

slots:
  currency:
    type: text
    influence_conversation: false
    mappings:
      - entity: currency
        type: from_entity

actions:
  - action_exchange_rate
